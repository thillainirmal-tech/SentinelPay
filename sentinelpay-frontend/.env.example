# SentinelPay Frontend — Environment Variables
# Copy to .env and fill in your values

# API Gateway base URL (all requests route through here)
REACT_APP_API_BASE_URL=http://localhost:8085

# App branding (optional overrides)
REACT_APP_APP_NAME=SentinelPay
REACT_APP_VERSION=1.0.0
